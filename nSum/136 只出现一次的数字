class Solution {
    public int singleNumber(int[] nums) {
        if(nums==null||nums.length==0) return 0;
        int xor=0;
        int len=nums.length;
        for(int i=0;i<len;i++){
            xor^=nums[i];
        }
        return xor;
    }
}
